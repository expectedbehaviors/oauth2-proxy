# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

- Switched baseline values from GitHub provider defaults to OIDC-first defaults (`provider=oidc` with `oidc_issuer_url` placeholder).
- Added Plex-backed OIDC documentation explaining required bridge components, 1Password fields, and deployment steps.
- Added feasibility matrix for Plex in other OIDC consumers (Portainer, Argo CD, Harbor).
